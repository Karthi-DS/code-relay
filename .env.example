# Backend Server Configuration
SERVER_PORT=5000
HOST=0.0.0.0
JWT_SECRET=codeblack_jwt_secret_key_2026

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=codeblack

# Connection String Alternative (Optional, overrides DB_* if set)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/codeblack
